// Hardcode the input values to avoid the ReferenceError
const posicaoInicial = 5; 
const totalPassos = 10; 

// Calcule a posição final do herói:
const posicaoFinal = posicaoInicial + totalPassos;

// Imprime a posição final
console.log("Posicao final do heroi:", posicaoFinal);
// Output: Posicao final do heroi: 15